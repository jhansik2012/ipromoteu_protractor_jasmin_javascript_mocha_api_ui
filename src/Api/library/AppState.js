
/**
 * Application specific simple global state.
 * It is primarily for memory cache.
 * Some AppState functions may also choose to persist the object in
 * browser local storage using ./LocalStore.js facility.
 *
 */

export const appState = {
};
